# --- Dépendances critiques ---
sgmllib3k
feedparser>=6.0.10
pydantic>=2.4.1

# --- Intelligence & Agents ---
crewai>=1.11.0
crewai-tools
langchain-groq
litellm
openai
groq

# --- Base de données ---
chromadb
pypdf
pdfminer.six

# --- Trading & Data ---
ccxt
pandas
numpy
requests
httpx
websocket-client

# --- Réseau & Bot ---
python-telegram-bot>=21.0
flask
prometheus-client

# --- Utilitaires ---
python-dotenv
gitpython
praw
matplotlib

# =================================================================
# === UPGRADES PHASE 1 (version finale build-stable) ==============
# =================================================================
vectorbt>=0.26.0          # Backtester vectorisé ultra-rapide (SMA, RSI, etc. intégrés)
backtrader                # Backtester classique de secours
pyfolio-reloaded          # Analyse performance (VaR, drawdown) – version maintenue
quantstats                # Alternative légère et moderne à pyfolio (très stable en 3.12)
redis                     # Cache hot data + positions
ccxt>=4.3.0               # Version plus récente pour OCO / reduce-only
aiohttp                   # Async pour WS & execution pro

# Note : pandas_ta supprimé définitivement car il cause des conflits de wheels dans Docker slim.
# VectorBT + quantstats couvrent largement tes besoins en backtest et analyse perf.
