# --- Dépendances critiques ---
sgmllib3k
feedparser>=6.0.10
pydantic>=2.4.1

# --- Intelligence & Agents ---
crewai>=1.11.0
crewai-tools
langchain-groq
litellm
openai
groq

# --- Base de données ---
chromadb
pypdf
pdfminer.six

# --- Trading & Data ---
ccxt
pandas
numpy
requests
httpx
websocket-client

# --- Réseau & Bot ---
python-telegram-bot>=21.0
flask
prometheus-client

# --- Utilitaires ---
python-dotenv
gitpython
praw
matplotlib

# =================================================================
# === UPGRADES PHASE 1 AJOUTÉES (corrigées pour build Docker) =====
# =================================================================
vectorbt>=0.26.0          # Backtester vectorisé ultra-rapide (inclut beaucoup d'indicateurs TA)
backtrader                # Backtester classique de secours
pyfolio>=0.9.2            # Analyse performance pro (VaR, drawdown) - version pinned
redis                     # Cache hot data + positions
ccxt>=4.3.0               # Version plus récente pour OCO / reduce-only / post-only
aiohttp                   # Async pour WS & execution pro

# Remplacement de pandas_ta (incompatible Python 3.11 / build slim)
# pandas-ta officiel nécessite Python >=3.12 et pose des problèmes de wheel
pandas-ta-remake          # Fork actif et compatible avec pandas + vectorbt
# pandas-ta-classic       # Alternative si tu veux l'ancien pandas-ta (décommente si besoin)

# Optionnel : pour encore plus de TA si tu installes TA-Lib manuellement plus tard
# ta-lib                  # Nécessite compilation supplémentaire
