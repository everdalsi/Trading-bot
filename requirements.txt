# --- Dépendances critiques ---
sgmllib3k
feedparser>=6.0.10
pydantic>=2.4.1

# --- Intelligence & Agents ---
crewai>=1.11.0
crewai-tools
langchain-groq
litellm
openai
groq

# --- Base de données ---
chromadb
pypdf
pdfminer.six

# --- Trading & Data ---
ccxt
pandas
numpy
requests
httpx
websocket-client

# --- Réseau & Bot ---
python-telegram-bot>=21.0
flask
prometheus-client

# --- Utilitaires ---
python-dotenv
gitpython
praw
matplotlib

# =================================================================
# === UPGRADES PHASE 1 (version build-stable & légère) ============
# =================================================================
vectorbt>=0.26.0          # Backtester vectorisé ultra-rapide (inclut la plupart des indicateurs TA dont tu as besoin)
backtrader                # Backtester classique de secours
pyfolio-reloaded          # Version maintenue et compatible de pyfolio (analyse perf + VaR)
redis                     # Cache hot data + positions
ccxt>=4.3.0               # Version plus récente pour OCO / reduce-only / post-only
aiohttp                   # Async pour WS & execution pro

# Note : pandas-ta supprimé pour éviter les conflits de wheels.
# VectorBT suffit largement pour tes backtests (SMA, RSI, Bollinger, etc.).
# Si tu veux plus d'indicateurs plus tard, on ajoutera TA-Lib manuellement.
